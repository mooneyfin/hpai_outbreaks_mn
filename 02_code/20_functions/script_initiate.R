# 0a. Centralized file path declarations
source(paste0(file.locations.folder, 'file_locations.R'))

# 0b. Load packages used across all analyses
source(paste0(packages.folder, 'packages_to_load.R'))

# 0c. Load shared helper functions (SPARK theme + palettes, lag-matrix helpers, etc.)
source(paste0(functions.folder, 'functions.R'))
